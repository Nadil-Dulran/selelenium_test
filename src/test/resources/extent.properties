# ExtentReports configuration for TestNG adapter
# Start Spark reporter and set output path
extent.reporter.spark.start=true
extent.reporter.spark.out=target/extent-reports/spark.html

# Optional: configure folder for screenshots if you link them in logs
screenshot.dir=target/surefire-reports/test-debug
screenshot.rel.path=./test-debug
